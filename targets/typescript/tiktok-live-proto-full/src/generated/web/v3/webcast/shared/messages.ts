// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               v5.27.5
// source: webcast/shared/messages.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { EmoteModel } from "../model/base/emoji.js";
import { ImageModel } from "../model/base/messages.js";
import { Text } from "../model/message/common.js";

export const protobufPackage = "webcast.shared";

export interface AnchorInfo {
  userId: string;
  defaultOrder: string;
}

export interface AuthenticationInfo {
  customVerify: string;
  enterpriseVerifyReason: string;
  authenticationBadge: ImageModel | undefined;
}

export interface DisplayConfig {
  showOnlinePanel: boolean;
  canPanelClick: boolean;
  showAvatarList: boolean;
  avatarCount: string;
  showScore: boolean;
}

export interface EmoteListResult {
  emoteList: EmoteModel[];
  exist: boolean;
}

export interface EmoteWithIndex {
  index: string;
  emote: EmoteModel | undefined;
}

export interface RightLabel {
  backgroundColor: string;
  content: Text | undefined;
  height: string;
}

export interface UserInfo {
  userId: string;
  nickName: string;
  avatarThumb: ImageModel | undefined;
}

function createBaseAnchorInfo(): AnchorInfo {
  return { userId: "0", defaultOrder: "0" };
}

export const AnchorInfo: MessageFns<AnchorInfo> = {
  encode(message: AnchorInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== "0") {
      writer.uint32(8).int64(message.userId);
    }
    if (message.defaultOrder !== "0") {
      writer.uint32(16).int64(message.defaultOrder);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AnchorInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAnchorInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.userId = reader.int64().toString();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.defaultOrder = reader.int64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseAuthenticationInfo(): AuthenticationInfo {
  return { customVerify: "", enterpriseVerifyReason: "", authenticationBadge: undefined };
}

export const AuthenticationInfo: MessageFns<AuthenticationInfo> = {
  encode(message: AuthenticationInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.customVerify !== "") {
      writer.uint32(10).string(message.customVerify);
    }
    if (message.enterpriseVerifyReason !== "") {
      writer.uint32(18).string(message.enterpriseVerifyReason);
    }
    if (message.authenticationBadge !== undefined) {
      ImageModel.encode(message.authenticationBadge, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AuthenticationInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAuthenticationInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.customVerify = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.enterpriseVerifyReason = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.authenticationBadge = ImageModel.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseDisplayConfig(): DisplayConfig {
  return { showOnlinePanel: false, canPanelClick: false, showAvatarList: false, avatarCount: "0", showScore: false };
}

export const DisplayConfig: MessageFns<DisplayConfig> = {
  encode(message: DisplayConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.showOnlinePanel !== false) {
      writer.uint32(8).bool(message.showOnlinePanel);
    }
    if (message.canPanelClick !== false) {
      writer.uint32(16).bool(message.canPanelClick);
    }
    if (message.showAvatarList !== false) {
      writer.uint32(24).bool(message.showAvatarList);
    }
    if (message.avatarCount !== "0") {
      writer.uint32(32).int64(message.avatarCount);
    }
    if (message.showScore !== false) {
      writer.uint32(40).bool(message.showScore);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DisplayConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDisplayConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.showOnlinePanel = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.canPanelClick = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.showAvatarList = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.avatarCount = reader.int64().toString();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.showScore = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseEmoteListResult(): EmoteListResult {
  return { emoteList: [], exist: false };
}

export const EmoteListResult: MessageFns<EmoteListResult> = {
  encode(message: EmoteListResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.emoteList) {
      EmoteModel.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.exist !== false) {
      writer.uint32(16).bool(message.exist);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EmoteListResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEmoteListResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.emoteList.push(EmoteModel.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.exist = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseEmoteWithIndex(): EmoteWithIndex {
  return { index: "0", emote: undefined };
}

export const EmoteWithIndex: MessageFns<EmoteWithIndex> = {
  encode(message: EmoteWithIndex, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.index !== "0") {
      writer.uint32(8).int64(message.index);
    }
    if (message.emote !== undefined) {
      EmoteModel.encode(message.emote, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EmoteWithIndex {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEmoteWithIndex();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.index = reader.int64().toString();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.emote = EmoteModel.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseRightLabel(): RightLabel {
  return { backgroundColor: "", content: undefined, height: "0" };
}

export const RightLabel: MessageFns<RightLabel> = {
  encode(message: RightLabel, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.backgroundColor !== "") {
      writer.uint32(10).string(message.backgroundColor);
    }
    if (message.content !== undefined) {
      Text.encode(message.content, writer.uint32(18).fork()).join();
    }
    if (message.height !== "0") {
      writer.uint32(24).int64(message.height);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RightLabel {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRightLabel();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.backgroundColor = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.content = Text.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.height = reader.int64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseUserInfo(): UserInfo {
  return { userId: "0", nickName: "", avatarThumb: undefined };
}

export const UserInfo: MessageFns<UserInfo> = {
  encode(message: UserInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== "0") {
      writer.uint32(8).int64(message.userId);
    }
    if (message.nickName !== "") {
      writer.uint32(18).string(message.nickName);
    }
    if (message.avatarThumb !== undefined) {
      ImageModel.encode(message.avatarThumb, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UserInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUserInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.userId = reader.int64().toString();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.nickName = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.avatarThumb = ImageModel.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
}
