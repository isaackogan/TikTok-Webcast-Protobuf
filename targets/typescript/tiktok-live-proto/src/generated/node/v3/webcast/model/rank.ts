// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               v7.34.0
// source: webcast/model/rank.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { ClassType, ProfitRankType } from "./data/messages.js";
import { Text } from "./message/common.js";

export const protobufPackage = "webcast.model.rank";

export interface ClassInfo {
  classType: ClassType;
  starCount: string;
}

export interface RankTabInfo {
  rankType: ProfitRankType;
  title: string;
  titleText: Text | undefined;
  listLynxType: string;
}

export interface SettleInfo {
  ownerSettleRank: string;
  newClassInfo: ClassInfo | undefined;
  oldClassInfo: ClassInfo | undefined;
  rankSettleTimestamp: string;
}

export interface TeamRankBonusTime {
  switch: boolean;
  startTimestamp: string;
  endTimestamp: string;
  showAnimation: boolean;
  bonusRate: string;
  bonusTimeGiftId: string;
}

function createBaseClassInfo(): ClassInfo {
  return { classType: 0, starCount: "0" };
}

export const ClassInfo: MessageFns<ClassInfo> = {
  encode(message: ClassInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.classType !== 0) {
      writer.uint32(8).int32(message.classType);
    }
    if (message.starCount !== "0") {
      writer.uint32(16).int64(message.starCount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ClassInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseClassInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.classType = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.starCount = reader.int64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseRankTabInfo(): RankTabInfo {
  return { rankType: 0, title: "", titleText: undefined, listLynxType: "0" };
}

export const RankTabInfo: MessageFns<RankTabInfo> = {
  encode(message: RankTabInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.rankType !== 0) {
      writer.uint32(8).int32(message.rankType);
    }
    if (message.title !== "") {
      writer.uint32(18).string(message.title);
    }
    if (message.titleText !== undefined) {
      Text.encode(message.titleText, writer.uint32(26).fork()).join();
    }
    if (message.listLynxType !== "0") {
      writer.uint32(32).int64(message.listLynxType);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RankTabInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRankTabInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.rankType = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.titleText = Text.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.listLynxType = reader.int64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseSettleInfo(): SettleInfo {
  return { ownerSettleRank: "0", newClassInfo: undefined, oldClassInfo: undefined, rankSettleTimestamp: "0" };
}

export const SettleInfo: MessageFns<SettleInfo> = {
  encode(message: SettleInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ownerSettleRank !== "0") {
      writer.uint32(8).int64(message.ownerSettleRank);
    }
    if (message.newClassInfo !== undefined) {
      ClassInfo.encode(message.newClassInfo, writer.uint32(18).fork()).join();
    }
    if (message.oldClassInfo !== undefined) {
      ClassInfo.encode(message.oldClassInfo, writer.uint32(26).fork()).join();
    }
    if (message.rankSettleTimestamp !== "0") {
      writer.uint32(32).int64(message.rankSettleTimestamp);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SettleInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSettleInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.ownerSettleRank = reader.int64().toString();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.newClassInfo = ClassInfo.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.oldClassInfo = ClassInfo.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.rankSettleTimestamp = reader.int64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseTeamRankBonusTime(): TeamRankBonusTime {
  return {
    switch: false,
    startTimestamp: "0",
    endTimestamp: "0",
    showAnimation: false,
    bonusRate: "0",
    bonusTimeGiftId: "0",
  };
}

export const TeamRankBonusTime: MessageFns<TeamRankBonusTime> = {
  encode(message: TeamRankBonusTime, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.switch !== false) {
      writer.uint32(8).bool(message.switch);
    }
    if (message.startTimestamp !== "0") {
      writer.uint32(16).int64(message.startTimestamp);
    }
    if (message.endTimestamp !== "0") {
      writer.uint32(24).int64(message.endTimestamp);
    }
    if (message.showAnimation !== false) {
      writer.uint32(32).bool(message.showAnimation);
    }
    if (message.bonusRate !== "0") {
      writer.uint32(40).int64(message.bonusRate);
    }
    if (message.bonusTimeGiftId !== "0") {
      writer.uint32(48).int64(message.bonusTimeGiftId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TeamRankBonusTime {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTeamRankBonusTime();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.switch = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.startTimestamp = reader.int64().toString();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.endTimestamp = reader.int64().toString();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.showAnimation = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.bonusRate = reader.int64().toString();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.bonusTimeGiftId = reader.int64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
}
