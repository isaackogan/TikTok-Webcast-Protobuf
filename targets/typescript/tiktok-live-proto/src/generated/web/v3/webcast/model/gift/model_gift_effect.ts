// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               v5.27.5
// source: webcast/model/gift/model_gift_effect.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { AssetsModel } from "./assets.js";
import { GiftEffectSpecs } from "./model.js";

export const protobufPackage = "webcast.model.gift.model";

export interface GiftEffect {
  assets: AssetsModel[];
  specs: GiftEffectSpecs | undefined;
  eventTrackingParams: { [key: string]: string };
}

export interface GiftEffect_EventTrackingParamsEntry {
  key: string;
  value: string;
}

function createBaseGiftEffect(): GiftEffect {
  return { assets: [], specs: undefined, eventTrackingParams: {} };
}

export const GiftEffect: MessageFns<GiftEffect> = {
  encode(message: GiftEffect, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.assets) {
      AssetsModel.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.specs !== undefined) {
      GiftEffectSpecs.encode(message.specs, writer.uint32(18).fork()).join();
    }
    globalThis.Object.entries(message.eventTrackingParams).forEach(([key, value]: [string, string]) => {
      GiftEffect_EventTrackingParamsEntry.encode({ key: key as any, value }, writer.uint32(42).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GiftEffect {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGiftEffect();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.assets.push(AssetsModel.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.specs = GiftEffectSpecs.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          const entry5 = GiftEffect_EventTrackingParamsEntry.decode(reader, reader.uint32());
          if (entry5.value !== undefined) {
            message.eventTrackingParams[entry5.key] = entry5.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseGiftEffect_EventTrackingParamsEntry(): GiftEffect_EventTrackingParamsEntry {
  return { key: "", value: "" };
}

export const GiftEffect_EventTrackingParamsEntry: MessageFns<GiftEffect_EventTrackingParamsEntry> = {
  encode(message: GiftEffect_EventTrackingParamsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GiftEffect_EventTrackingParamsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGiftEffect_EventTrackingParamsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
}
